class Author {
    firstName: String;
    LastName: String;

    constructor(
        firstName: String,
        LastName: String,
    ) {
        this.firstName = firstName;
        this.LastName = LastName;
    }
}